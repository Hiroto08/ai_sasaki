# 本人許諾済みの写真はリポジトリにコミットしない（ローカル配置のみ）
public/avatar.png
public/*.png

node_modules/
.env

# 音声合成のキャッシュ（本人の声のため共有しない）
data/cache/

# 制作素材（本人の顔・声を含む／大きなバイナリ）
# assets/README.md だけは共有し、中身はコミットしない
assets/*
!assets/README.md
# QRは本人の情報を含まず、再生成の手間を省きたいので共有する
!assets/qr/
