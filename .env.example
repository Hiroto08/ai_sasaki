# このファイルを .env にコピーして値を設定する（.env はコミットされない）
#   cp .env.example .env

# Google Gemini APIキー（未設定ならデモモードで動作）
# 取得：https://aistudio.google.com/apikey
GEMINI_API_KEY=

# 使用モデル（省略可）
MODEL=gemini-2.5-flash

# ポート（省略可）
PORT=3000
